void tactic() {//ворота ->мяч
  /* if езда на мяч
     else if захвачен мяч
     else посиск мяча
  */
//  if (ball_retention < 1) { //фототранзистор для определения в захвате мяч
//    State = 2;
//  }
//  else if (ball_dist == 0) //езда
//    State = 0;
//  else //поиск мяча
//    State = 1;
//
//
//
//  if (State == 1) {
//    //ехать на мяч
//    if (ball_dist <= 40) {
//      //вкл дриблер
//    }
//    goAngle(0, exponential_detour(ball_angle, ball_dist, 0.45, 0.4, 0.35, 0.7), 110);
//
//  }
//  else if (State == 2) { //в ворота
//    if (blue_dist <= 50) {
//      //kick();
//    }
//    goAngle(0, blue_angle, 130);
//  }
//  else { //поиск мяча
//    goAngle(0, 0, 100);
//  }
}
